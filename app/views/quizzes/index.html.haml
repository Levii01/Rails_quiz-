%h1 Listing quizzes

%table
  %thead
    %tr
      %th Title
      %th Description
      %th
      %th
      %th

  %tbody
    - @quizzes.each do |quizz|
      %tr
        %td= quizz.title
        %td= quizz.description
        %td= link_to 'Show', quizz
        %td= link_to 'Edit', edit_quizz_path(quizz)
        %td= link_to 'Destroy', quizz, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Quizz', new_quizz_path
